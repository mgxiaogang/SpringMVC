<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.liupeng.dao.sqlexec.SqlExecutorMapper">
    <select id="sqlExecutorSelect" resultType="java.util.Map">
		${sql}
	</select>

    <insert id="sqlExecutorInsert">
		${sql}
	</insert>

    <update id="sqlExecutorUpdate">
		${sql}
	</update>
</mapper>